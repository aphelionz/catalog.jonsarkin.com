<?php
$this->pageTitle('Rapid Editor');
$this->headLink()->appendStylesheet($this->assetUrl('css/editor.css', 'RapidEditor') . '?v=0.2.0');
$this->headScript()->appendFile($this->assetUrl('js/editor.js', 'RapidEditor') . '?v=0.2.0');
// Hide the admin page title and reclaim full #content area
$this->headStyle()->appendStyle('#content { padding: 0 !important; } #content > h1.page-title { display: none; }');
?>

<div class="rapid-editor-container">

  <!-- ── Nav bar ──────────────────────────────────────────────────────── -->
  <nav id="nav">
    <div class="nav-left">
      <span id="queue-status">Loading…</span>
    </div>
    <div class="nav-center">
      <a id="item-link" href="#" target="_blank" title="Open in Omeka admin"></a>
    </div>
    <div class="nav-right">
      <div class="filter-buttons">
        <div class="filter-split">
          <button class="filter-btn active" data-filter="issues">Issues <span id="count-issues"></span></button>
          <button class="filter-caret" id="sprint-caret" title="Sprint: fix one field at a time">▾</button>
          <div id="sprint-menu" class="sprint-menu hidden"></div>
        </div>
        <button class="filter-btn" data-filter="exhibit">Exhibit</button>
      </div>
    </div>
    <div id="progress-bar"><div id="progress-fill"></div></div>
  </nav>

  <!-- ── Loading overlay ──────────────────────────────────────────────── -->
  <div id="loading">
    <div class="loading-inner">
      <div class="spinner"></div>
      <p id="loading-text">Loading catalog items…</p>
    </div>
  </div>

  <!-- ── Main layout ──────────────────────────────────────────────────── -->
  <main id="main" class="hidden">

    <!-- Image panel -->
    <section id="image-panel">
      <img id="item-image" alt="Artwork image">
      <div id="image-loading" class="hidden">Loading image…</div>
    </section>

    <!-- Form panel -->
    <section id="form-panel">

      <!-- Sprint view — one-property-at-a-time editing -->
      <div id="sprint-view">
        <div id="sprint-view-progress">
          <span id="sprint-view-field-label"></span>
          <span id="sprint-view-count"></span>
          <button id="sprint-view-undo" class="btn btn-nav" disabled>Undo</button>
        </div>
        <div id="sprint-view-meta"></div>
        <div id="sprint-view-current"></div>
        <div id="sprint-view-zone"></div>
        <div id="sprint-view-actions">
          <button type="button" id="sprint-view-skip" class="btn btn-skip">Skip</button>
        </div>
        <div id="sprint-view-done" class="hidden"></div>
      </div>
    </section>
  </main>

  <!-- ── Curate mode ────────────────────────────────────────────────── -->
  <section id="curate-panel" class="hidden">
    <div id="curate-progress">
      <span id="curate-count"></span>
      <button id="curate-undo" class="btn btn-nav" disabled>Undo</button>
    </div>
    <div id="card-stage"></div>
    <div id="curate-actions">
      <button id="curate-pass" class="btn curate-btn-pass" title="← or swipe left">← Pass</button>
      <button id="curate-keep" class="btn curate-btn-keep" title="→ or swipe right">Keep →</button>
    </div>
  </section>

  <!-- Toast -->
  <div id="toast" class="hidden"></div>

</div>
