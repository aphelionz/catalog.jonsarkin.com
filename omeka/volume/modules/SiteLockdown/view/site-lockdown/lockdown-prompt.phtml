<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow">
    <title>Access Required</title>
    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background: #f5f5f5;
            color: #333;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 1rem;
        }
        .card {
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.1);
            padding: 2rem;
            width: 100%;
            max-width: 380px;
        }
        .card h1 {
            font-size: 1.125rem;
            font-weight: 600;
            margin-bottom: 1.25rem;
            text-align: center;
        }
        .field { margin-bottom: 1rem; }
        .field input {
            width: 100%;
            padding: 0.625rem 0.75rem;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            outline: none;
            transition: border-color 0.15s;
        }
        .field input:focus { border-color: #555; }
        .error {
            color: #c0392b;
            font-size: 0.875rem;
            margin-bottom: 1rem;
            text-align: center;
        }
        button {
            width: 100%;
            padding: 0.625rem;
            font-size: 1rem;
            font-weight: 500;
            color: #fff;
            background: #333;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.15s;
        }
        button:hover { background: #555; }
    </style>
</head>
<body>
    <div class="card">
        <h1>Access Required</h1>
        <?php if (!empty($error)): ?>
            <p class="error">Incorrect password.</p>
        <?php endif; ?>
        <form method="post">
            <div class="field">
                <input type="password" name="lockdown_password" placeholder="Password" autofocus required>
            </div>
            <button type="submit">Enter</button>
        </form>
    </div>
</body>
</html>
