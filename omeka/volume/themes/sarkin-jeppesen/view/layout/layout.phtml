<?php
/**
 * Sarkin-Jeppesen — Master layout
 * Jeppesen manual-inspired wrapper for the Jon Sarkin Catalog Raisonne.
 */
$escape = $this->plugin('escapeHtml');
$site   = $this->site;

$this->htmlElement('html')->setAttribute('lang', $this->lang());
$this->headMeta()->setCharset('utf-8');
$this->headMeta()->appendName('viewport', 'width=device-width, initial-scale=1');
$this->headTitle($site->title())->setSeparator(' · ');

// Theme assets
$this->headLink()->prependStylesheet($this->assetUrl('css/style.css'), 'all');
// Omeka core icons (needed for admin bar)
$this->headLink()->appendStylesheet($this->assetUrl('css/iconfonts.css', 'Omeka'));

// jQuery + Omeka core
$this->headScript()->prependFile($this->assetUrl('vendor/jquery/jquery.min.js', 'Omeka'));
$this->headScript()->prependFile($this->assetUrl('js/global.js', 'Omeka'));
$this->jsTranslate();

// Theme JS (loaded at end of body)
$this->inlineScript()->appendFile($this->assetUrl('js/qrcode.min.js'));
$this->inlineScript()->appendFile($this->assetUrl('js/sarkin.js'));

$this->trigger('view.layout');

$footerText = $this->themeSetting('footer_text')
    ?: '&copy; ' . date('Y') . ', The Jon Sarkin Estate / Artists Rights Society (ARS), New York. All rights reserved.';

// Estate public key from ShieldedRegistryAssets ceremony
$sraPublicKey = null;
try {
    $sraResult = $this->setting('sra_ceremony_result');
    if (is_string($sraResult)) {
        $sraResult = json_decode($sraResult, true);
    }
    if (is_array($sraResult) && !empty($sraResult['pk_r'])) {
        $sraPublicKey = $sraResult['pk_r'];
    }
} catch (\Exception $e) { /* module not installed or setting missing */ }
?>
<!DOCTYPE html>
<?php echo $this->htmlElement('html'); ?>
<head>
    <script>(function(){var c=document.cookie.match('(?:^|; )sarkin-theme=([^;]*)');var t=(c&&c[1])||localStorage.getItem('sarkin-theme')||((window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches)?'dark':'light');document.documentElement.setAttribute('data-theme',t);document.cookie='sarkin-theme='+t+';domain=.jonsarkin.com;path=/;max-age=31536000;SameSite=Lax';})()</script>
    <?php echo $this->headMeta(); ?>
    <?php echo $this->headTitle(); ?>
    <?php echo $this->headLink(); ?>
    <?php echo $this->headStyle(); ?>
    <?php echo $this->headScript(); ?>
    <?php echo $this->placeholder('json_ld'); ?>
</head>

<?php echo $this->htmlElement('body'); ?>
    <a id="skipnav" href="#content" class="sr-only"><?php echo $this->translate('Skip to main content'); ?></a>

    <?php echo $this->userBar(); ?>

    <div class="site-wrapper">
        <header class="site-header">
            <a href="<?php echo $escape($site->url()); ?>" class="site-title-link">
                <img src="<?php echo $this->assetUrl('img/JS-Signature.png'); ?>" alt="<?php echo $escape($site->title()); ?>" class="site-logo">
            </a>
            <div class="site-search-group">
                <form class="site-search" action="<?php echo $this->url('site/resource', ['controller' => 'index', 'action' => 'search'], true); ?>">
                    <input type="text" name="fulltext_search" placeholder="Search" aria-label="Search" />
                    <button type="submit" aria-label="Search">
                        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
                    </button>
                </form>
                <?php try { $advancedUrl = $this->url('site/visual-search', ['site-slug' => $site->slug()]); ?>
                <div class="site-search-advanced">
                    <a href="<?php echo $this->url('site/collector-submit', ['site-slug' => $site->slug()]); ?>" class="pill-button">[submit]</a>
                    <a href="#" class="pill-button" id="global-share">[share]</a>
                    <a href="<?php echo $advancedUrl; ?>" class="pill-button">[visual search]</a>
                    <a href="#" class="pill-button" id="theme-toggle">[dark]</a>
                </div>
                <?php } catch (\Exception $e) { /* SimilarPieces not installed */ } ?>
            </div>
            <button class="nav-toggle" aria-label="Menu" aria-expanded="false" aria-controls="site-nav">
                <span></span><span></span><span></span>
            </button>
        </header>

        <?php
        // Mark "Catalog" nav active on submit page (custom controller, not a site page)
        $isSubmitPage = strpos($this->serverUrl(true), '/submit') !== false;
        ?>
        <nav id="site-nav" class="site-nav<?php echo $isSubmitPage ? ' submit-active' : ''; ?>" aria-label="<?php echo $this->translate('Primary navigation'); ?>">
            <?php echo $site->publicNav()->menu()->renderMenu(null, [
                'maxDepth' => 1,
            ]); ?>
        </nav>

        <div class="mobile-nav" id="mobile-nav" role="dialog" aria-label="Navigation menu">
            <button class="mobile-nav__close" aria-label="Close menu">&times;</button>
            <form class="mobile-nav__search" action="<?php echo $this->url('site/resource', ['controller' => 'index', 'action' => 'search'], true); ?>">
                <input type="text" name="fulltext_search" placeholder="Search" aria-label="Search" />
                <button type="submit" aria-label="Search">
                    <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
                </button>
            </form>
            <?php try { $mobileAdvancedUrl = $this->url('site/visual-search', ['site-slug' => $site->slug()]); ?>
            <div class="mobile-nav__actions">
                <a href="<?php echo $this->url('site/collector-submit', ['site-slug' => $site->slug()]); ?>">[submit]</a>
                <a href="#" id="mobile-share">[share]</a>
                <a href="<?php echo $mobileAdvancedUrl; ?>">[visual search]</a>
                <a href="#" id="mobile-theme-toggle">[dark]</a>
            </div>
            <?php } catch (\Exception $e) { /* SimilarPieces not installed */ } ?>
            <?php
            $navContainer = $site->publicNav()->getContainer();
            foreach ($navContainer as $page) {
                $label  = $escape($page->getLabel());
                $href   = $escape($page->getHref());
                $children = $page->getPages();
                if (count($children)) {
                    echo '<div class="mobile-nav__group">';
                    echo '<a href="' . $href . '" class="mobile-nav__parent">' . $label . '</a>';
                    echo '<div class="mobile-nav__sub">';
                    foreach ($children as $child) {
                        echo '<a href="' . $escape($child->getHref()) . '">' . $escape($child->getLabel()) . '</a>';
                    }
                    echo '</div></div>';
                } else {
                    echo '<a href="' . $href . '">' . $label . '</a>';
                }
            }
            ?>
        </div>

        <main id="content" role="main">
            <?php echo $this->content; ?>
        </main>

        <footer class="site-footer">
            <div class="footer-line">
                <div class="footer-text">
                    <p><?php echo $footerText; ?></p>
                    <p>For inquiries about Jon Sarkin's work, contact Mark Henderson, Estate Manager, at <a href="mailto:art@jonsarkin.com">art@jonsarkin.com</a>.</p>
                </div>
                <div class="footer-right">
                    <div class="footer-social">
                        <a href="https://instagram.com/jonsarkin" target="_blank" rel="noopener noreferrer" aria-label="Instagram" class="social-icon social-icon--instagram">
                            <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="20" rx="5" ry="5"/><circle cx="12" cy="12" r="5"/><line x1="17.5" y1="6.5" x2="17.51" y2="6.5"/></svg>
                        </a>
                        <a href="https://youtube.com/@jonsarkin" target="_blank" rel="noopener noreferrer" aria-label="YouTube" class="social-icon social-icon--youtube">
                            <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M22.54 6.42a2.78 2.78 0 0 0-1.94-2C18.88 4 12 4 12 4s-6.88 0-8.6.46a2.78 2.78 0 0 0-1.94 2A29 29 0 0 0 1 11.75a29 29 0 0 0 .46 5.33A2.78 2.78 0 0 0 3.4 19.1c1.72.46 8.6.46 8.6.46s6.88 0 8.6-.46a2.78 2.78 0 0 0 1.94-2 29 29 0 0 0 .46-5.25 29 29 0 0 0-.46-5.43z"/><polygon points="9.75 15.02 15.5 11.75 9.75 8.48 9.75 15.02"/></svg>
                        </a>
                        <a href="https://substack.com/@jonsarkin" target="_blank" rel="noopener noreferrer" aria-label="Substack" class="social-icon social-icon--substack">
                            <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M22.539 8.242H1.46V5.406h21.08v2.836zM1.46 10.812V24L12 18.11 22.54 24V10.812H1.46zM22.54 0H1.46v2.836h21.08V0z"/></svg>
                        </a>
                        <a href="https://x.com/jonsarkin" target="_blank" rel="noopener noreferrer" aria-label="X" class="social-icon social-icon--x">
                            <svg aria-hidden="true" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
                        </a>
                    </div>
                    <nav class="footer-links" aria-label="Footer">
                        <a href="<?php echo $this->url('site/page', ['site-slug' => $site->slug(), 'page-slug' => 'rights']); ?>">Rights &amp; Reproduction</a>
                        <a href="<?php echo $this->url('site/page', ['site-slug' => $site->slug(), 'page-slug' => 'terms']); ?>">Terms of Use</a>
                        <a href="<?php echo $this->url('site/page', ['site-slug' => $site->slug(), 'page-slug' => 'contact']); ?>">Contact</a>
                    </nav>
                </div>
            </div>
            <?php if ($sraPublicKey): ?>
            <p class="estate-key">Estate public key: <code><?php echo $escape($sraPublicKey); ?></code></p>
            <?php endif; ?>
        </footer>
    </div>

    <?php echo $this->inlineScript(); ?>
</body>
</html>
