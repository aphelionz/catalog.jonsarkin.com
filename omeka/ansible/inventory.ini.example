[omeka]
# catalog.jonsarkin.com ansible_host=YOUR_VPS_IP ansible_user=ubuntu
# catalog.jonsarkin.com ansible_host=YOUR_VPS_IP ansible_user=root

[omeka:vars]
# Optional: set SSH key path if not default
# ansible_ssh_private_key_file=~/.ssh/id_rsa
