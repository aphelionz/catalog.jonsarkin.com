services:
  traefik:
    image: traefik:v2.11.32
    restart: always
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.email=mark@mrh.io"
      - "--certificatesresolvers.letsencrypt.acme.storage=/acme.json"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
      - "--log.level=WARN"
      - "--accesslog=true"
      - "--ping=true"
      - "--entrypoints.websecure.transport.respondingTimeouts.readTimeout=600s"
      - "--entrypoints.websecure.transport.respondingTimeouts.writeTimeout=600s"
      - "--entrypoints.web.transport.respondingTimeouts.readTimeout=600s"
      - "--entrypoints.web.transport.respondingTimeouts.writeTimeout=600s"
      - "--serversTransport.forwardingTimeouts.dialTimeout=300s"
      - "--serversTransport.forwardingTimeouts.responseHeaderTimeout=600s"
      - "--serversTransport.forwardingTimeouts.idleConnTimeout=600s"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./acme.json:/acme.json

  db:
    image: mariadb:10.11
    restart: unless-stopped
    environment:
      MARIADB_DATABASE: ${MYSQL_DATABASE}
      MARIADB_USER: ${MYSQL_USER}
      MARIADB_PASSWORD: ${MYSQL_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - dbdata:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--su-mysql", "--connect"]
      interval: 10s
      retries: 30

  omeka:
    image: giocomai/omeka-s-docker:v4.2.0
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./omeka/volume:/var/www/html/volume
      - ./omeka/php-uploads.ini:/usr/local/etc/php/conf.d/uploads.ini:ro
      - ./omeka/reqtimeout.conf:/etc/apache2/mods-enabled/reqtimeout.conf:ro
      - ./omeka/apache-expires.load:/etc/apache2/mods-enabled/expires.load:ro
      - ./omeka/apache-headers.load:/etc/apache2/mods-enabled/headers.load:ro
      - ./omeka/apache-caching.conf:/etc/apache2/conf-enabled/caching.conf:ro
      - /var/www/omeka-s/files:/var/www/html/volume/files
    environment:
      MYSQL_HOST: db
      MYSQL_PORT: 3306
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      OMEKA_ADMIN_EMAIL: ${OMEKA_ADMIN_EMAIL:-admin@example.com}
      OMEKA_SITE_TITLE: "Jon Sarkin Catalog"
      OMEKA_ENVIRONMENT: production
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
    labels:
      traefik.enable: "true"
      traefik.http.routers.omeka-http.entrypoints: "web"
      traefik.http.routers.omeka-http.rule: "Host(`catalog.jonsarkin.com`)"
      traefik.http.routers.omeka-http.middlewares: "omeka-https-redirect"
      traefik.http.middlewares.omeka-https-redirect.redirectscheme.scheme: "https"
      traefik.http.routers.omeka.entrypoints: "websecure"
      traefik.http.routers.omeka.rule: "Host(`catalog.jonsarkin.com`)"
      traefik.http.routers.omeka.tls: "true"
      traefik.http.routers.omeka.tls.certresolver: "letsencrypt"
      traefik.http.routers.omeka.middlewares: "omeka-compress,omeka-headers"
      traefik.http.middlewares.omeka-compress.compress: "true"
      traefik.http.middlewares.omeka-headers.headers.customresponseheaders.X-Robots-Tag: "noimageai, noimageindex"
      traefik.http.services.omeka.loadbalancer.server.port: "80"

  qdrant:
    image: qdrant/qdrant:v1.16
    restart: unless-stopped
    volumes:
      - qdrant_data:/qdrant/storage
      - qdrant_snapshots:/qdrant/snapshots

  clip-api:
    image: aphelionz/sarkin-clip-api
    restart: unless-stopped
    depends_on:
      - qdrant
    env_file: .env
    environment:
      QDRANT_URL: http://qdrant:6333
      QDRANT_COLLECTION: omeka_items
      VECTOR_NAME: visual_vec
      TEXT_VECTOR_NAME: text_vec_clip
      SEARCH_MODE: hybrid
      SEARCH_DB_PATH: /app/search_index/index.sqlite
      DEFAULT_LIMIT: 50
      DINO_COLLECTION: sarkin_motif_patches_518
      DINO_ENABLED: "true"
      SEGMENT_ENABLED: "true"
      SEGMENT_INGEST_ENABLED: "false"
      SEGMENT_COLLECTION: sarkin_motif_segments
    volumes:
      - search_index:/app/search_index
      - /opt/catalog/segments:/app/segments

volumes:
  dbdata:
  qdrant_data:
  qdrant_snapshots:
  search_index:
